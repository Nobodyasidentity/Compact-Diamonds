name: Sync Language Files to Resourcepack

on:
  push:
    paths:
      - 'assets/compact_diamonds/lang/**'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout datapack repo
        uses: actions/checkout@v4

      - name: Clone resourcepack repository
        run: |
          git clone https://x-access-token:${{ secrets.REPO_TOKEN }}@github.com/Nobodyasidentity/Compact-Diamonds-Translations.git resourcepack

      - name: Copy language files
        run: |
          mkdir -p resourcepack/assets/compact_diamonds/lang
          rm -rf resourcepack/assets/compact_diamonds/lang/*
          cp -r assets/compact_diamonds/lang/* resourcepack/assets/compact_diamonds/lang/

      - name: Commit and push if there are changes
        run: |
          cd resourcepack
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git diff --cached --quiet || git commit -m "Auto-sync language files from datapack"
          git push
